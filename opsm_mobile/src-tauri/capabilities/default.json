{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "OPSM Mobile default permissions - network for API, path for lockfile audit",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:path:default",
    "log:default"
  ]
}
